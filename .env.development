# Vite env for LOCAL DEVELOPMENT ONLY (npm run dev).
# Loaded automatically by the dev server; production builds ignore it.
# Production (Render) needs no VITE_API_URL — the SPA is same-origin.
VITE_API_URL=http://localhost:8000
