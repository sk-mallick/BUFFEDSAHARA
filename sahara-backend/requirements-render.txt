# Render runtime dependencies — full API, WITHOUT the optional
# torch/transformers sentiment model (~2 GB). The app boots without them:
# /api/health reports model_loaded=false and the transparent keyword
# fallback serves sentiment. Add torch/transformers here if you accept a
# multi-GB image and slower cold starts.
fastapi>=0.115.6
uvicorn[standard]>=0.32.1
motor>=3.6.1
pydantic>=2.10.4
pydantic-settings>=2.7.1
python-dotenv>=1.0.1
bcrypt>=4.2.1
PyJWT>=2.9.0
anthropic>=0.25.0
langdetect>=0.9.0
google-genai>=1.0.0
numpy>=2.1,<3
